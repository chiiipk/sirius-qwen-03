# requirements.txt (phiên bản cuối cùng, đã sửa lỗi torch)

transformers>=4.42.0
accelerate
# THAY ĐỔI: Nâng cấp torch và thêm các thư viện liên quan
torch>=2.3.1
torchvision>=0.18.1
torchaudio>=2.3.1

scikit-learn==1.4.2
einops
# llm2vec và flash-attn đã được xóa để cài riêng hoặc bỏ qua
openai==1.51.0
nltk==3.8.1
retry==0.9.2
