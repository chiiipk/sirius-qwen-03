# Nâng cấp transformers để hỗ trợ Qwen3 và các model mới nhất
transformers>=4.42.0

# Thêm accelerate để tối ưu việc tải và chạy model trên nhiều thiết bị (GPU/CPU)
accelerate

# Thư viện torch và các thư viện khác
torch==2.3.0
scikit-learn==1.4.2
einops
llm2vec==0.2.2
flash-attn --no-build-isolation
openai==1.51.0
nltk==3.8.1
retry==0.9.2
